{
  "rules": {
    "users": {
      ".write": false,
          ".read":true,
      "$uid": {
        ".write": "auth != null && auth.uid == $uid",
          ".read":"auth != null && auth.uid == $uid"
      }
    },
      "Match":{
         ".write": true,
          ".read":true
      }
  }
}

